---
// Hero Section - Bold futuristic hero with image
import { Image } from "astro:assets";
import ResumeButton from "../ResumeButton";
const profileImage = "https://youssef.tn/ysf.webp";
---

<section
    class="relative lg:min-h-screen flex items-center pt-32 pb-20 lg:py-20 overflow-hidden"
    id="home"
>
    <!-- Grid Background -->
    <div class="absolute inset-0 grid-bg opacity-50"></div>

    <div class="container-main relative z-10">
        <div
            class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-center"
        >
            <!-- Left Column - Content -->
            <div
                class="order-1 lg:order-1 text-center lg:text-left flex flex-col items-center lg:items-start"
            >
                <!-- Status Badge -->
                <div class="mb-8">
                    <div
                        class="flex items-center gap-3 justify-center lg:justify-start"
                    >
                        <span class="w-2 h-2 bg-accent animate-pulse"></span>
                        <span
                            class="text-sm uppercase tracking-[0.2em] text-muted"
                            >Hello, I'm</span
                        >
                    </div>
                </div>

                <!-- Main Headline -->
                <noscript>
                    <h1 class="display-xl mb-6">Youssef Dhibi</h1>
                    <p class="text-xl md:text-2xl text-muted max-w-xl mb-12 leading-relaxed">
                        Full Stack Web & Mobile Developer (iOS & Android) Based in Tunisia.
                    </p>
                </noscript>
                <h1
                    class="display-xl mb-6 flex flex-col items-center lg:items-start whitespace-nowrap"
                >
                    <span class="block">Youssef</span>
                    <div class="inline-block relative">
                        <span class="text-outline-hover">
                            <span class="outline-text">Dhibi</span>
                            <span class="filled-text" aria-hidden="true"
                                >Dhibi</span
                            >
                        </span>
                    </div>
                </h1>

                <div class="text-xl md:text-2xl text-muted max-w-xl mb-12 leading-relaxed mx-auto lg:mx-0 lg:justify-start justify-center text-center lg:text-left">
                    Full-stack developer building mobile and web apps from Tunisia. 17 projects, 5-star reviews.
                </div>

                <!-- CTA Button -->
                <div>
                    <div
                        class="flex flex-wrap gap-4 justify-center lg:justify-start"
                    >
                        <a
                            href="#projects"
                            class="btn-primary"
                            id="hero-cta-primary"
                        >
                            <span>View My Work</span>
                            <svg
                                class="w-4 h-4"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                            >
                                <path
                                    stroke-linecap="square"
                                    stroke-linejoin="miter"
                                    stroke-width="2"
                                    d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </a>
                        <ResumeButton client:load />
                    </div>
                </div>
            </div>

            <!-- Right Column - Image -->
            <div
                class="relative order-2 lg:order-2 flex justify-center lg:justify-end"
            >
                <div>
                    <div class="relative">
                        <!-- Wireframe Background -->
                        <div
                            class="absolute -inset-8 wireframe-visual opacity-20"
                        >
                        </div>

                        <!-- Geometric Frame -->
                        <div class="absolute -inset-4 border border-white/10">
                        </div>
                        <div class="absolute -inset-8 border border-white/5">
                        </div>

                        <!-- Blue Accent Corner -->
                        <div
                            class="absolute -top-4 -left-4 w-8 h-8 border-t-2 border-l-2 border-accent"
                        >
                        </div>
                        <div
                            class="absolute -bottom-4 -right-4 w-8 h-8 border-b-2 border-r-2 border-accent"
                        >
                        </div>

                        <!-- Image Container -->
                        <div
                            class="relative w-72 h-80 md:w-80 md:h-96 lg:w-96 lg:h-[28rem] overflow-hidden"
                        >
                            <Image
                                src={profileImage}
                                alt="Youssef Dhibi"
                                width={500}
                                height={600}
                                class="w-full h-full object-cover object-top transition-all duration-500"
                                loading="eager"
                            />
                            <!-- Overlay -->
                            <div
                                class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent"
                            >
                            </div>
                        </div>

                        <!-- Name Tag -->
                        <div
                            class="absolute -bottom-6 left-0 right-0 flex justify-center"
                        >
                            <div
                                class="px-6 py-3 bg-black border border-white/10"
                            >
                                <span
                                    class="text-xs uppercase tracking-[0.3em] text-muted"
                                    >Youssef Dhibi</span
                                >
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Scroll Indicator - Hidden on mobile -->
    <div
        class="absolute bottom-8 left-1/2 -translate-x-1/2 hidden lg:flex flex-col items-center gap-2 scroll-indicator"
    >
        <span class="text-xs uppercase tracking-widest text-muted">Scroll</span>
        <svg
            class="w-4 h-4 text-muted"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
        >
            <path
                stroke-linecap="square"
                stroke-linejoin="miter"
                stroke-width="2"
                d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
    </div>

    <!-- Decorative Lines -->
    <div class="absolute left-8 top-1/3 hidden lg:block">
        <div
            class="w-px h-32 bg-gradient-to-b from-transparent via-white/20 to-transparent"
        >
        </div>
    </div>
</section>

<style>
    .font-display {
        font-family: var(--font-display);
    }

    .border-accent {
        border-color: var(--accent);
    }
</style>
